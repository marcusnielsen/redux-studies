import {name} from './constants'

export const add = `${name}/add`
export const remove = `${name}/remove`
export const edit = `${name}/edit`
export const complete = `${name}/complete`
export const completeAll = `${name}/complete_all`
export const clearCompleted = `${name}/clear_completed`
